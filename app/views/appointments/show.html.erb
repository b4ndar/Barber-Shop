<p id="notice"><%= notice %></p>


<div class="w-1/3 max-w-1/3 md:w-1/3 p-6 mx-auto mt-32 border">
			<div class=" bg-white">
					<p class="w-full text-gray-600 text-3xl text-center">Appointment</p>
          <p class="text-center">Date:</p>
					<p class="border-b text-center"><%= current_user.name %></p>
          <p class="text-center">Barber:</p>
          <p class="border-b text-center"><%= @appointment.date %></p>
          <p class="text-center">Customer:</p>
          <p class="border-b text-center"><%= @appointment.time %></p>
        <hr>
			</div>
			<div class="flex justify-center mt-auto bg-white rounded-b rounded-t-none overflow-hidden shadow p-6">
        <button class="mx-auto lg:mx-0 hover:underline bg-green-700 font-bold rounded-full my-6 py-4 px-8 shadow-lg"><%= link_to 'Edit', edit_appointment_path(@appointment) %></button>
        <button class="mx-auto lg:mx-0 hover:underline bg-yellow-500 font-bold rounded-full my-6 py-4 px-8 shadow-lg"><%= link_to 'Back', appointments_path %></button>
			</div>
		</div>
